<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Variante dark del tema base: sin ActionBar y con status / navigation
         bar oscuras. Compose sigue manejando todo el contenido vía BaropodTheme. -->
    <style name="Theme.Baropod" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
